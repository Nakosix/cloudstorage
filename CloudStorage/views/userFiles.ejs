<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Файлы пользователя <%= userId %></title>
</head>
<body>

    <h1>Файлы пользователя <%= userId %></h1>
    <h2>Список файлов:</h2>

    <ul>
        <% files.forEach(function(file) { %>
            <li>
                <a href="/download/<%= userId %>/<%= file %>">Скачать <%= file %></a>
                <a href="/delete/<%= userId %>/<%= file %>">Удалить</a>
            </li>
        <% }); %>
    </ul>

    <br>
    <a href="/admin">Назад в админку</a>

</body>
</html>
